pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          //node --version
          git --version
          java --version
          curl --version
          mvn --version
        '''
      }
    }
  }
}
